services:
  - type: web
    name: trackguard-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn crcts_proj.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
